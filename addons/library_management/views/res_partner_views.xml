<?xml version="1.0" encoding="utf-8"?>
<odoo>
    
    <!-- Inherit Contact Form View to Add My Book Field -->
    <record id="view_partner_form_my_book" model="ir.ui.view">
        <field name="name">res.partner.form.my.book</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <!-- Add My Book field after the Tags field -->
            <xpath expr="//field[@name='category_id']" position="after">
                <field name="my_book_ids" string="My Book" nolabel="1">
                    <list string="My Book">
                        <field name="title"/>
                        <field name="author_ids" widget="many2many_tags"/>
                        <field name="publisher_id"/>
                        <field name="date_published"/>
                    </list>
                </field>
            </xpath>
        </field>
    </record>

</odoo>
